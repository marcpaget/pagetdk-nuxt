import { sessionStorage } from "./sessionStorage.mjs";
export const getStorageSession = (sessionId) => sessionStorage.getItem(sessionId);
export const setStorageSession = (sessionId, session) => sessionStorage.setItem(sessionId, session);
export const dropStorageSession = (sessionId) => sessionStorage.removeItem(sessionId);
