"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports._on = void 0;
exports.addEventListenerBinding = addEventListenerBinding;
exports.attachEventBinding = attachEventBinding;
exports.on = void 0;
function addEventListenerBinding(element, event, handler) {
  if (element && event && handler) {
    element.addEventListener(event, handler, false);
  }
}
function attachEventBinding(element, event, handler) {
  if (element && event && handler) {
    element.attachEvent("on" + event, handler);
  }
}
const _on = () => {
  if (typeof window === "undefined") {
    return () => {};
  }
  if (window.document.addEventListener) {
    return addEventListenerBinding;
  } else {
    return attachEventBinding;
  }
};
exports._on = _on;
const on = _on();
exports.on = on;