"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.once = void 0;
var _off = require("./off");
var _on = require("./on");
const once = function (el, event, fn) {
  const listener = function (...args) {
    if (fn) {
      fn.apply(this, args);
    }
    (0, _off.off)(el, event, listener);
  };
  (0, _on.on)(el, event, listener);
};
exports.once = once;