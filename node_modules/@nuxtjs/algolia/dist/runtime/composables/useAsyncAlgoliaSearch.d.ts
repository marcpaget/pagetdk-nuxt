import type { RequestOptionsObject } from '../../types';
export declare type SearchParams = {
    query: string;
    indexName?: string;
    key?: string;
} & RequestOptionsObject;
export declare function useAsyncAlgoliaSearch({ query, requestOptions, indexName, key }: SearchParams): Promise<any>;
