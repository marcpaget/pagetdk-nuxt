import type { createViewportManager } from './manager';
/**
 * Viewport manager instance.
 */
export declare type ViewportManager = ReturnType<typeof createViewportManager>;
/**
 * Viewport options.
 */
export declare type ViewportOptions = {
    /**
     * Breakpoints.
     */
    breakpoints: Record<string, number>;
    /**
     * Cookie name.
     *
     * Default: "viewport".
     */
    cookieName: string;
    /**
     * Default breakpoints based on device type for auto detection.
     *
     * Available devices: "console", "desktop", "embedded", "mobile", "smarttv", "tablet", "wearable".
     */
    defaultBreakpoints: Record<string, string>;
    /**
     * Fallback breakpoint.
     */
    fallbackBreakpoint: string;
};
/**
 * Viewport query.
 */
export declare type ViewportQuery = {
    /**
     * CSS media query.
     */
    mediaQuery: string;
    /**
     * Size.
     */
    size: number;
};
declare module 'vue/types/vue' {
    interface Vue {
        $viewport: ViewportManager;
    }
}
declare module '#app' {
    interface NuxtApp extends PluginInjection {
    }
}
interface PluginInjection {
    $viewport: ViewportManager;
}
declare module '@vue/runtime-core' {
    interface ComponentCustomProperties extends PluginInjection {
    }
}
export {};
